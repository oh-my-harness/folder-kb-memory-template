# 验证记录

2026-09-23，本地 Python 3.11 / Windows：

- 24 项单元测试，23 项通过；1 项因当前 Windows 进程不能创建符号链接而跳过。CI 配置 Ubuntu 和 Windows，会在支持链接的环境执行该项。
- 离线完整演示通过：初始化、两条记忆、观察待审、明确纠错、原材料重放；重放后候选工作区摘要完全一致。
- 平台真实协议联调通过：注册、安装 ZIP、绑定工作区、收件箱收到 3 条材料、运行已安装程序、平台结果校验通过；原快照不变、辅助文件保留。
- DeepSeek 接口实测通过：两条自然语言材料生成两条带证据的记忆，一条观察进入待审；再次提交相同材料未调用模型且候选不变。
- GLM 接口本轮返回 HTTP 502，重试后任务失败，没有输出可发布的 result.json。尚不能据此判断 GLM 服务恢复后的整理效果。
- 已通过管理 REST API 安装到本机运行中的平台，创建并绑定独立的“个人记忆演示”知识库。仅包含上述合成示例，未调整其他知识库与 Key 授权。更新仍需命令行执行。

这些检查验证程序和平台接口能连通，不等同于完成长期记忆质量评测。冲突判定、事实准确性和纠错质量仍需真实材料验证及发布审核。
